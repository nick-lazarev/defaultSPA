import { Stack } from "@mui/material";
import { LibList } from "@widgets";

export const IndexPage = () => {
  return (
    <Stack spacing={4}>
      <h1>Default SPA</h1>
      <LibList />
    </Stack>
  );
};
